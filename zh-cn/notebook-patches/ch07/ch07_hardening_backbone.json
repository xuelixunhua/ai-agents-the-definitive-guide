{
  "notebook": "ch07/ch07_hardening_backbone.ipynb",
  "translation": "zh-CN",
  "policy": {
    "preserve_code_logic": true,
    "preserve_api_names": true,
    "preserve_class_and_function_names": true,
    "preserve_prompts": true,
    "preserve_saved_outputs": true,
    "first_term_occurrence_bilingual": true
  },
  "markdown_cells": {
    "_mAwJrvBoA2g": "# 在 Colab 中打开\n<a target=\"_blank\" href=\"https://colab.research.google.com/github/Nicolepcx/ai-agents-the-definitive-guide/blob/main/CH07/ch07_hardening_backbone.ipynb\">\n  <img src=\"https://colab.research.google.com/assets/colab-badge.svg\" alt=\"Open In Colab\"/>\n</a>",
    "YDxGM11ouSVf": "# 关于本 Notebook\n\n从诊断实验室走向生产 API，意味着你要从 **\"agent vibes（凭感觉做智能体）\"** 进入 **\"system physics（系统工程约束）\"**。本 Notebook 实现了让智能体能够在生产环境中持续可靠运行的三个基础支柱：\n\n1. **检查点（Checkpointing）** —— 周期性保存状态快照，让智能体在进程重启后可以恢复。\n2. **上下文裁剪（Context Pruning）** —— 按 token 预算截断历史，避免“记忆膨胀”。\n3. **熔断器（Circuit Breaker）** —— 依据轮次和成本设置保险丝，阻止失控循环。\n\n| 模式 | 关注点 | 机制 | 为什么需要 |\n|---|---|---|---|\n| Checkpointing | 可靠性 | 周期性把状态快照保存到磁盘 / Redis | 如果进程在 10 步中的第 4 步崩溃，智能体可以从第 4 步恢复，而不是从头开始。 |\n| Context Pruning | 性能 | 按 token 预算裁剪历史 | 防止“上下文膨胀（Context Bloat）”：陈旧且无关的消息会削弱推理质量并显著增加成本。 |\n| Circuit Breaker | 安全 | 按轮次和成本设置熔断条件 | 当智能体陷入工具调用失败的无限循环，或超过预算时及时停止。 |\n\n我们会把这三种模式组合成一个**加固后的智能体循环（hardened agent loop）**，再通过实际演示验证每一种机制都能生效。",
    "SADEYpsHuSVk": "### 1. `AgentSession` 让恢复成为可能\n通过 Pydantic 把智能体的完整状态变成可序列化对象，你就可以在每一轮结束后保存快照。如果进程在 10 步中的第 4 步崩溃，恢复时可以直接从第 4 步继续，而不是从零开始。生产环境通常会把状态写入 Redis 或 Postgres，而不是 JSON 文件。\n\n### 2. 上下文裁剪不是可选项\n随着上下文持续增长，模型会变慢，也更容易失去重点。Context Janitor 会保留系统 Prompt、保留最近轮次，并对被丢弃的内容进行摘要。它不是简单的“清理”，而是一种性能与成本控制机制。\n\n### 3. 为每个 Session 硬编码预算上限\n熔断器是防御无限循环的第一道防线。一个卡在工具调用循环中的智能体，几分钟就可能耗掉大量 API 预算。为每个 Session 设置 0.50 美元的上限，是成本很低的保险措施。当熔断器触发时，FastAPI 层可以捕获异常，并返回用户友好的“我需要人工介入”提示。\n\n### 4. 这些模式可以组合\nCheckpointing、Context Pruning 和 Circuit Breaker 不是彼此独立的功能，而是同一个循环中的多层保护。每一轮都执行：裁剪 → 调用 → 更新指标 → 检查熔断器 → 保存检查点。缺少任何一层，系统都会留下盲区。\n\n### 5. 流式传输（Streaming）与轮询（Polling）\n在 FastAPI 的生产部署中，长时间运行的智能体任务应尽量避免使用普通 REST `POST` 请求。智能体需要时间“思考”，30 秒超时并不少见。可以使用 **WebSockets** 或 **服务器发送事件（Server-Sent Events，SSE）** 把智能体的执行过程持续推送到 UI。它不会让模型本身变快，但用户能够看到智能体正在工作，从而降低感知延迟。",
    "slCd9oTquSVh": "## 智能体工具（Agent Tools）\n\n在生产环境中，这些函数可能会调用订单数据库、策略服务和工单分类器。这里使用带有真实感数据的确定性替代实现，使加固模式能够被测试并复现；但工具 Schema、Dispatcher（分发器）以及函数调用契约（function-calling contract）的结构，与真正上线时采用的方式是一致的。",
    "QD4nrKRfuSVi": "# `AgentSession`：可序列化状态（Serializable State）\n\n`AgentSession` 让智能体的完整状态可以被序列化。它就是你需要保存到 Redis、Postgres，或者本演示中的磁盘 JSON 文件里的“状态大脑”。\n\n所有与恢复有关的重要字段都包含在这里：对话历史、轮次计数、累计成本以及 Session 状态。如果进程退出，只要重新加载这个对象，就可以从中断的位置继续执行。",
    "pYJyT9uLuSVi": "# Token 计数与成本跟踪（Token Counting & Cost Tracking）\n\n这里有两个不同任务，因此也需要两个不同的事实来源：\n\n1. **调用前（上下文管理）：** 在发送请求之前，我们要知道历史消息会占用多少 token，Context Janitor 才能判断是否需要裁剪。这里使用 `tiktoken`——与模型一致的 BPE（Byte Pair Encoding）分词器——进行精确计数。\n2. **调用后（成本跟踪）：** 每次 API 响应后，OpenRouter 会返回真实的 `usage.prompt_tokens` 和 `usage.completion_tokens`。计费应使用这些实际用量，而不是本地估算值。",
    "tt92EAg5uSVj": "# Context Janitor：历史裁剪（History Pruning）\n\n高端模型虽然拥有很大的上下文窗口，但把窗口完全塞满通常是错误做法。它会增加延迟，也会让智能体的注意力被“摊薄”。Context Janitor 通过下面三条规则保持历史精简：\n\n1. **永远不裁剪系统 Prompt** —— 它包含智能体最核心的身份和行为约束。\n2. **只保留最近的若干轮次** —— 保住最直接、最相关的上下文。\n3. **为被裁剪内容注入摘要** —— 在缩短上下文的同时保留长期信息。",
    "KWbwMLCHuSVj": "# 熔断器（Circuit Breaker）\n\n为每个 Session 硬编码预算上限，是防止无限循环的核心保护。这里的熔断器包含两根“保险丝”：\n\n- **预算保险丝（Budget Fuse）：** 如果 Session 的累计成本超过 `BUDGET_CAP`，立即停止。\n- **轮次保险丝（Turn Fuse）：** 如果智能体的推理轮次超过 `MAX_TURNS`，立即停止。\n\n熔断器触发后会抛出 `CircuitBreakerTripped` 异常。FastAPI 层可以捕获这个异常，并转换成用户友好的“我卡住了，需要人工介入”提示。",
    "dRtMigWGuSVj": "# Checkpointing：保存与恢复（Save & Restore）\n\n每一轮结束后保存检查点，可以避免 502 错误或容器重启迫使用户从头再来。生产环境通常会把检查点写入 Redis 或 Postgres；为了让演示中的状态快照可以直接检查，这里使用 JSON 文件。",
    "ZJlIBa0OuSVj": "# 加固后的智能体循环（Hardened Agent Loop）\n\n这里把三种模式真正组合起来。循环的每一次迭代都会：\n\n1. 在调用模型之前**裁剪上下文**（Context Janitor）\n2. **调用 LLM**，并执行任何工具调用\n3. **更新指标**（token、成本、轮次计数）\n4. **检查熔断器**（预算 + 轮次）\n5. **保存检查点**（状态持久化）\n\n即使任意一步失败，最后一次成功保存的检查点仍然可以用于恢复。",
    "yHYq6JY_uSVj": "---\n\n## 演示：三种模式协同工作\n\n下面让加固后的智能体依次通过四个场景，以验证每种机制都能真正生效：\n\n1. **正常的多轮运行** —— 每一轮结束后保存检查点\n2. **触发上下文裁剪** —— 人为膨胀历史，再观察 Context Janitor 如何裁剪\n3. **触发熔断器** —— 设置很低的轮次上限，观察保险丝何时熔断\n4. **崩溃恢复（Crash Recovery）** —— “杀掉”当前 Session，再从最后一个检查点恢复",
    "tdO8PCWKuSVj": "### 演示 1：正常的多轮运行\n\n这是一段接近真实的客服对话：客户有一个延迟订单，询问退货政策，然后继续追问。智能体会调用工具、每轮保存检查点，同时熔断器保持未触发状态。",
    "dQ3oUiCUuSVk": "### 演示 2：上下文裁剪生效\n\n我们故意向历史中塞入 20 条填充消息，再发送一个真实问题。Context Janitor 应当在调用 LLM 之前清理冗余内容，同时保留系统 Prompt 和最近的对话轮次。",
    "T-uGrxq7uSVk": "### 演示 3：熔断器触发\n\n我们把 `max_turns=3`，然后尝试发送 5 条消息。熔断器应在达到限制时触发，使 Session 进入停止状态并抛出 `CircuitBreakerTripped`。在 FastAPI 应用中，你可以捕获这个异常，并返回用户友好的“需要转人工”提示。",
    "vUmZs7VpuSVk": "### 演示 4：从检查点进行崩溃恢复\n\n我们通过“忘掉”当前 Session 对象来模拟进程崩溃，然后从最后一个检查点恢复。智能体会从原位置继续运行：对话历史、指标和轮次计数都保持一致。",
    "ZsCVNHs8uSVk": "---\n\n## 汇总仪表盘（Summary Dashboard）",
    "lnRSEM-SuSVk": "## 清理（Cleanup）"
  },
  "code_comment_replacements": {
    "RYcRq18ouSVh": [
      {"old": "# tiktoken encoding matched to the model — this gives us *exact* token\n# counts for pre-call context management, not the chars÷4 guesswork\n# you see in tutorials.", "new": "# 使用与模型匹配的 tiktoken 编码，以获得调用前上下文管理所需的精确 token 计数，\n# 而不是教程中常见的 chars÷4 粗略估算。"}
    ],
    "y7_vPxoEuSVi": [
      {"old": "# Tool definitions (OpenAI function-calling schema)", "new": "# 工具定义（OpenAI function-calling schema）"},
      {"old": "# Mock data", "new": "# 模拟数据"},
      {"old": "# Mock tool implementations", "new": "# 模拟工具实现"},
      {"old": "# Dispatcher", "new": "# 分发器（Dispatcher）"},
      {"old": "\"\"\"Dispatch a tool call by name. Returns a JSON string result.\"\"\"", "new": "\"\"\"按名称分发工具调用，并返回 JSON 字符串结果。\"\"\""},
      {"old": "# Quick smoke test", "new": "# 快速冒烟测试"}
    ],
    "osnFRbN5uSVi": [
      {"old": "\"\"\"Serializable agent state — the brain you checkpoint.\"\"\"", "new": "\"\"\"可序列化的智能体状态——需要保存到检查点里的“状态大脑”。\"\"\""},
      {"old": "\"\"\"Append a message to the conversation history.\"\"\"", "new": "\"\"\"向对话历史追加一条消息。\"\"\""},
      {"old": "# Quick sanity check", "new": "# 快速健全性检查"}
    ],
    "MJDw7z8suSVi": [
      {"old": "#  Pre-call: tiktoken-based token counting", "new": "# 调用前：基于 tiktoken 的 token 计数"},
      {"old": "# Per-message overhead (role, delimiters) — 4 tokens for gpt-4o/gpt-4o-mini", "new": "# 每条消息的固定开销（role、分隔符）；gpt-4o / gpt-4o-mini 为 4 个 token"},
      {"old": "# tool_calls, repro_steps, etc.", "new": "# tool_calls、repro_steps 等"},
      {"old": "# reply primer", "new": "# 回复起始标记（reply primer）"},
      {"old": "#  Post-call: cost from actual API-reported usage", "new": "# 调用后：根据 API 返回的真实用量计算成本"},
      {"old": "# gpt-4o-mini pricing (per 1K tokens) — update these when pricing changes", "new": "# gpt-4o-mini 价格（每 1K token）；价格变化时需要同步更新"},
      {"old": "\"\"\"Calculate USD cost from the API-reported token counts.\"\"\"", "new": "\"\"\"根据 API 返回的 token 用量计算美元成本。\"\"\""},
      {"old": "#  Demo: show the difference between real counting and guessing", "new": "# 演示：比较精确计数与粗略估算的差异"}
    ],
    "RlYTWh3WuSVj": [
      {"old": "# Never prune the system prompt (first message)", "new": "# 永远不裁剪系统 Prompt（第一条消息）"},
      {"old": "# Build a summary of what we're pruning", "new": "# 为即将被裁剪的内容生成摘要"},
      {"old": "# Demo: build a fat history and prune it", "new": "# 演示：构造膨胀的历史并执行裁剪"},
      {"old": "return history  # Nothing to prune", "new": "return history  # 无需裁剪"}
    ],
    "WLx8h-QOuSVj": [
      {"old": "\"\"\"Raised when the agent exceeds its operational budget or turn limit.\"\"\"", "new": "\"\"\"当智能体超过运行预算或轮次上限时抛出。\"\"\""},
      {"old": "# Demo: force a trip", "new": "# 演示：主动触发熔断"},
      {"old": "# push over the $0.50 cap", "new": "# 推高到 0.50 美元上限之上"}
    ],
    "Dh_uujRJuSVj": [
      {"old": "\"\"\"Persist the full session state to disk, counting this checkpoint.\"\"\"", "new": "\"\"\"把完整 Session 状态持久化到磁盘，并计入本次检查点。\"\"\""},
      {"old": "\"\"\"Restore a session from its last checkpoint. Returns None if not found.\"\"\"", "new": "\"\"\"从最近一次检查点恢复 Session；若不存在则返回 None。\"\"\""},
      {"old": "\"\"\"List all saved session IDs.\"\"\"", "new": "\"\"\"列出所有已保存的 Session ID。\"\"\""},
      {"old": "# Demo", "new": "# 演示"}
    ],
    "9bCurq0uuSVj": [
      {"old": "# Safety cap on tool-call loops within a single turn", "new": "# 单轮工具调用循环的安全上限"},
      {"old": "# Ensure system prompt is in history", "new": "# 确保系统 Prompt 已写入历史"},
      {"old": "# Add the user message", "new": "# 添加用户消息"},
      {"old": "#  Context Janitor: prune before calling the model", "new": "# Context Janitor：调用模型前先裁剪上下文"},
      {"old": "#  LLM call (with tool-call loop)", "new": "# LLM 调用（包含工具调用循环）"},
      {"old": "#  Update metrics", "new": "# 更新指标"},
      {"old": "# No tool calls → final answer", "new": "# 没有工具调用 → 得到最终答案"},
      {"old": "# Execute tool calls", "new": "# 执行工具调用"},
      {"old": "#  Increment turn counter", "new": "# 增加轮次计数"},
      {"old": "#  Circuit Breaker", "new": "# 熔断器（Circuit Breaker）"},
      {"old": "#  Checkpoint", "new": "# 保存检查点（Checkpoint）"}
    ],
    "-5UNjr6HuSVk": [
      {"old": "# Inject the system prompt", "new": "# 注入系统 Prompt"},
      {"old": "# Bloat the history with 20 turns of filler", "new": "# 用 20 轮填充内容刻意膨胀历史"},
      {"old": "# Force aggressive pruning", "new": "# 强制执行更激进的裁剪"}
    ],
    "ldOCJMtHuSVk": [
      {"old": "# This should trigger the breaker", "new": "# 这条消息应触发熔断器"},
      {"old": "# Should never reach this", "new": "# 正常情况下不应执行到这里"}
    ],
    "hSec1B7VuSVk": [
      {"old": "# Step 1: Start a session and run 2 turns", "new": "# 第 1 步：启动 Session，并运行 2 轮"},
      {"old": "# Step 2: Simulate the crash", "new": "# 第 2 步：模拟崩溃"},
      {"old": "# Gone. Process died. Container restarted.", "new": "# 对象已消失：进程退出，容器重启"},
      {"old": "# Step 3: Recover from checkpoint", "new": "# 第 3 步：从检查点恢复"},
      {"old": "# Step 4: Continue the conversation", "new": "# 第 4 步：继续对话"}
    ],
    "JwlJYrxquSVk": [
      {"old": "# Gather all sessions we can find in checkpoints", "new": "# 汇总检查点中能够找到的所有 Session"}
    ],
    "S9cQ6ClguSVk": [
      {"old": "# Clean up the temporary checkpoint directory", "new": "# 清理临时检查点目录"}
    ]
  }
}
