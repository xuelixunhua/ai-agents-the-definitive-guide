{
  "notebook": "CH05/ch05_pydantic_agent_consistency.ipynb",
  "translation": "zh-CN",
  "policy": {
    "preserve_code_logic": true,
    "preserve_api_names": true,
    "preserve_class_and_function_names": true,
    "preserve_prompts": true,
    "preserve_saved_outputs": true,
    "first_term_occurrence_bilingual": true
  },
  "markdown_cells": {
    "qOv41Qft0AY7": "# 关于本 Notebook\n\n本 Notebook 给出了一套构建生产级 AI 智能体（AI Agents）的蓝图，重点解决从“在我的机器上能跑”到“在大规模生产环境中可靠运行”之间的关键差距。\n\n**问题：** 智能体在生产环境中经常会静默失败（silent failure）。当 LLM 输出出现轻微变化、JSON 解析失败，或不同提供商返回不一致的数据类型（例如本应是整数却返回字符串）时，往往会导致大量时间被浪费在脆弱代码的调试上。\n\n**解决方案：** 使用 **Pydantic 模型（Pydantic models）** 作为技术契约（technical contracts），在系统边界明确规定数据的精确结构。这样可以阻止无效状态继续传播，简化模型提供商切换，并让系统具备更强的自恢复能力。\n\n---\n\n## 面向生产环境的技术支柱\n\n### 1. 确定性的策略管理（Deterministic Policy Management）\n为了保证一次运行从开始到结束保持稳定，验证策略必须在**运行开始时固定（pin）**。\n* **策略哈希（Policy Hashing）：** 对当前生效的配置进行哈希，并把结果保存到运行状态中。\n* **一致性（Consistency）：** 即使部署过程中全局配置发生变化，已经开始的运行仍继续遵循初始化时的规则。\n* **审计轨迹（Audit Trails）：** 每条数据库记录都包含 `policy_hash`，开发者可以据此准确追溯生成该数据时使用了哪套验证规则。\n\n### 2. 面向性能优化的验证\n验证本身不应成为性能瓶颈。\n* **模型缓存（Model Caching）：** Pydantic 模型针对每个 `(mode, policy_hash)` 组合只构建一次并缓存，避免在“热路径（hot paths）”中反复生成类。\n* **直接传递字典：** 节点把原始数据直接传给工厂函数，避免在执行图内部增加不必要的解析开销。\n\n### 3. 显式状态管理（Explicit State Management）\n与其通过错误次数推断是否成功，不如使用**布尔验证标志**（例如 `prompt_ok`、`image_ok`）。\n* **明确的失败模式：** 路由逻辑直接检查这些标志。\n* **易读日志：** 重试逻辑先递增计数，再判断是否超过上限，因此日志可以清楚展示“Attempt 1”到“Attempt N”。\n\n### 4. 边界分离（Boundary Separation）\n严格区分**工具结果（Tool Results）**与**数据库记录（Database Records）**。\n* **契约独立性：** 验证工具输出的 Schema 可以独立于永久存储所使用的 Schema 演进。\n* **类型安全契约：** 数据库记录接收的是已经验证过的 `BaseModel` 实例，而不是原始字典或 `Any` 类型。\n\n---\n\n## 架构设计模式\n\n实现采用**三块式结构（Three-Block Structure）**：\n\n| 组件 | 职责 |\n| :--- | :--- |\n| **Governance Config** | 使用 OmegaConf 集中管理策略，并支持 Strict / Lenient 等模式切换。 |\n| **Contracts & Gates** | 通过缓存的 Pydantic 工厂构建防御性边界，验证不可信数据。 |\n| **Graph Wiring** | 使用 LangGraph 工作流管理状态持久化、检查点（checkpointer）和重试逻辑。 |\n\n---\n\n## Pydantic 与 Instructor：策略层面对比\n\nPydantic 主要保护图内部的系统边界，而 **Instructor** 则把恢复机制进一步推近到生成源头。\n\n| 特性 | LangGraph + Pydantic | Instructor |\n| :--- | :--- | :--- |\n| **恢复逻辑** | 通过 refine 节点把恢复放在**图（Graph）**中。 | 通过自动重试把恢复放在**客户端（Client）**中。 |\n| **可见性** | 每一次重试都是 Trace 中独立的节点。 | 重试在 LLM 调用内部完成。 |\n| **复杂度** | 较高，需要条件路由和循环。 | 较低，从生成到下一步可以保持线性流程。 |\n| **更适合** | 复杂多智能体系统以及审计要求较高的任务。 | 结构化数据抽取与简单的“修复后重试”循环。 |\n\n---\n\n## 关键实现细节\n\n本 Notebook 演示了几个对生产环境非常重要的实现细节：\n1. **稳定的 Schema 哈希（Stable Schema Hashing）：** 动态生成的模型使用显式 title，确保 JSON Schema 引用在不同进程之间保持稳定。\n2. **积极归一化（Aggressive Normalization）：** 在哈希前，对验证关键词和策略值排序并转成小写，避免无意义的缓存失效。\n3. **可追溯性（Traceability）：** `run_id` 把每条数据库记录关联到具体的 LangGraph `thread_id` 与 checkpoint 状态。",
    "W7vMlIQHfJ2X": "# 导入依赖",
    "9DDjy-750AY9": "## 问题：静默失败（Silent Failures）\n\n先看一个常见场景：一个处理用户数据的智能体。如果不使用 Pydantic，类型不匹配可能不会立即报错，而是静默地进入后续流程，或者在更深层的位置触发晦涩错误。\n\n下面展示一种典型的静默失败：字符串（String）与整数（Integer）的混用。数据库期望整数，但 LLM 返回了字符串。",
    "ak3uB7YW0AY9": "## 解决方案：Pydantic 验证（Pydantic Validation）\n\nPydantic 会在系统边界**立即**捕获这些错误，而不是让错误继续向系统内部传播。",
    "om3_vAs10AY9": "## 模型提供商差异：真实世界的问题\n\n不同 LLM 提供商返回的 JSON 结构往往会有细微差异。如果没有验证层，就需要针对每个提供商编写单独的解析逻辑。",
    "Rhg8iq2g0AY_": "## 为什么 Pydantic 对智能体很重要\n\n1. **防止静默失败：** 在系统边界捕获类型不匹配，而不是等错误深入代码后才暴露\n2. **提供商一致性：** 一个模型可以吸收不同提供商之间的常见差异，例如字符串与整数、不同字段名等\n3. **类型安全（Type Safety）：** IDE 和类型检查器能够理解你的数据结构\n4. **自描述（Self-Documenting）：** 模型本身就是规范，不需要另外维护一套文档\n5. **LangGraph 集成：** 验证后的 State 能避免无效数据在智能体步骤之间传播\n\n## 最佳实践\n\n- **在边界定义模型：** 在数据进入系统的位置进行建模，例如 LLM 响应、API 调用和用户输入\n- **使用 Field 约束：** 通过 `ge`、`le`、`pattern` 等参数表达验证规则\n- **吸收提供商差异：** 利用 Pydantic 的类型强制转换（type coercion）处理常见差异\n- **快速失败（Fail Fast）：** 让验证错误尽早暴露，不要在深层逻辑中尝试“修补”无效数据",
    "kG27C2srfAmd": "# 整合所有概念",
    "O57S58NFfsJe": "## 治理配置（Governance Config）与策略选择",
    "5Rx5JKcvfxza": "## 契约与验证门（Contracts and Gates，带缓存）",
    "zqlCOIvrf_9r": "## 构建节点",
    "b_fBXq9IgRNs": "## 构建图（Graph）",
    "ox7ypn_IgZ0P": "## 运行应用",
    "5apZf0JY79PZ": "## Pydantic 与 Instructor：边界保护 vs. 源头恢复\n\n在这个 LangGraph 工作流中，Pydantic 扮演的是边界验证门（boundary gate）。它可以阻止无效数据继续传播，但恢复逻辑仍然存在于编排层，例如独立的 refine 节点以及显式的重试路由。\n\n当你希望把恢复机制进一步推近生成源头时，Instructor 会非常有用。它不再把结构化输出验证视为独立的工作流问题，而是把 Schema 约束与生成过程绑定在一起：模型会基于同一套固定的 Pydantic 契约反复尝试生成合格输出，并把验证错误作为反馈。这样可以减少图中的分支、去掉特定的“修复器（fixer）”节点，并让不同模型与提供商之间的结构化输出行为更加一致。\n\n换句话说，Pydantic 负责保护系统边界；Instructor 则减少稳定命中这道边界所需要的额外工作。",
    "e6CeOkZ5guQI": "# Instructor：在生成源头恢复（Recovery at the Source）",
    "vGWEjOBFgzif": "### 复用前面相同的 Pydantic 模型（沿用策略驱动的模型）",
    "onrb55b8g8p3": "# 使用 Instructor 简化 LangGraph 工作流"
  },
  "code_comment_replacements": {
    "qAyGthrUfIzb": [
      {"old": "# Standard library", "new": "# Python 标准库"},
      {"old": "# LangChain / LangGraph", "new": "# LangChain / LangGraph 相关依赖"},
      {"old": "# Third party", "new": "# 第三方依赖"}
    ],
    "uEpftrDu0AY9": [
      {"old": "\"\"\"Without validation, this fails silently or crashes unpredictably.\"\"\"", "new": "\"\"\"没有验证时，这段逻辑可能静默失败，或以不可预测的方式崩溃。\"\"\""},
      {"old": "# Could be \"123\" (string) or 123 (int)", "new": "# 既可能是 \"123\"（字符串），也可能是 123（整数）"},
      {"old": "# Could be \"25\" or 25", "new": "# 既可能是 \"25\"，也可能是 25"},
      {"old": "# This works in staging (maybe the test data is correct)", "new": "# 在预发布环境中可能正常，因为测试数据恰好是正确类型"},
      {"old": "# But fails in production when the LLM returns strings", "new": "# 但当生产环境中的 LLM 返回字符串时就可能失败"},
      {"old": "# TypeError if user_id is \"123\"", "new": "# 如果 user_id 是 \"123\"，行为就与整数完全不同"},
      {"old": "# SQL injection risk if age is string", "new": "# 如果 age 是未经约束的字符串，还可能引入 SQL 注入风险"},
      {"old": "# Simulate what happens when an LLM returns inconsistent data", "new": "# 模拟 LLM 返回不一致数据类型时的情况"},
      {"old": "# ✅ Works (staging)", "new": "# ✅ 可正常工作（预发布）"},
      {"old": "# ❌ Fails silently (production)", "new": "# ❌ 在生产环境中可能静默地产生错误结果"}
    ],
    "D2u8PJcg0AY9": [
      {"old": "# Define the exact shape of your data", "new": "# 精确定义数据结构"},
      {"old": "\"\"\"Technical spec: defines exactly what data we expect.\"\"\"", "new": "\"\"\"技术规范：精确定义系统期望接收的数据。\"\"\""},
      {"old": "# Automatically convert strings to ints when possible", "new": "# 在可行时自动把字符串转换为整数"},
      {"old": "# This handles provider differences gracefully", "new": "# 用这种方式优雅吸收不同提供商之间的差异"},
      {"old": "\"\"\"With Pydantic, validation happens at the boundary.\"\"\"", "new": "\"\"\"使用 Pydantic 后，验证会发生在系统边界。\"\"\""},
      {"old": "# Validation happens here - fails fast if data is wrong", "new": "# 在这里完成验证：数据错误时立即失败（fail fast）"},
      {"old": "# Now we can trust the types", "new": "# 通过验证后，就可以信任这些数据类型"},
      {"old": "# Same test cases", "new": "# 使用相同的测试用例"},
      {"old": "# Strings - Pydantic converts them!", "new": "# 字符串：Pydantic 会完成转换"},
      {"old": "# Invalid - Pydantic catches it!", "new": "# 无效数据：Pydantic 会立即捕获"}
    ],
    "6Znz4vGY0AY-": [
      {"old": "# Simulate different provider responses", "new": "# 模拟不同模型提供商返回的响应"},
      {"old": "\"\"\"Unified model that works across all providers.\"\"\"", "new": "\"\"\"可跨不同提供商复用的统一模型。\"\"\""},
      {"old": "# Provider A: Returns nested structure", "new": "# Provider A：返回一种结构"},
      {"old": "# String instead of int", "new": "# 返回字符串而不是整数"},
      {"old": "# Provider B: Returns flat structure with different field names", "new": "# Provider B：返回字段命名不同的扁平结构"},
      {"old": "# Comma-separated string instead of list", "new": "# 返回逗号分隔字符串，而不是列表"},
      {"old": "# Different key name", "new": "# 使用了不同的字段名"},
      {"old": "# Provider C: Returns valid JSON but missing fields", "new": "# Provider C：JSON 合法，但缺少部分字段"},
      {"old": "# Missing tags and metadata", "new": "# 缺少 tags 和 metadata"},
      {"old": "\"\"\"Without Pydantic, you need custom parsing for each provider.\"\"\"", "new": "\"\"\"如果没有 Pydantic，就需要为不同提供商分别编写解析逻辑。\"\"\""},
      {"old": "\"\"\"With Pydantic, one model handles all providers.\"\"\"", "new": "\"\"\"使用 Pydantic 后，可以用同一个模型处理不同提供商。\"\"\""},
      {"old": "# Pydantic automatically:", "new": "# Pydantic 会自动完成："},
      {"old": "# - Converts string \"3\" to int 3", "new": "# - 把字符串 \"3\" 转成整数 3"},
      {"old": "# - Handles missing fields (uses defaults)", "new": "# - 处理缺失字段（使用默认值）"},
      {"old": "# - Validates types and constraints", "new": "# - 校验类型与约束"},
      {"old": "# Handle provider B's comma-separated tags", "new": "# 处理 Provider B 返回的逗号分隔 tags"},
      {"old": "# Handle provider B's different metadata key", "new": "# 处理 Provider B 不同的 metadata 字段名"}
    ],
    "gxovexDRfu1q": [
      {"old": "# Runtime mode: \"strict\", \"moderate\", or \"lenient\"", "new": "# 运行模式：\"strict\"、\"moderate\" 或 \"lenient\""},
      {"old": "# Mode-specific policies", "new": "# 各模式对应的策略"},
      {"old": "# Real constraint: no code blocks in prompts", "new": "# 真实约束：Prompt 中不允许代码块"},
      {"old": "# Must include style descriptor", "new": "# 必须包含风格描述词"},
      {"old": "\"\"\"Get the active policy based on current mode.\"\"\"", "new": "\"\"\"根据当前模式获取生效策略。\"\"\""},
      {"old": "\"\"\"Generate deterministic hash of policy for versioning.\"\"\"", "new": "\"\"\"为策略生成确定性哈希，用于版本追踪。\"\"\""}
    ],
    "E7YqKqVRf9MY": [
      {"old": "# Cache Pydantic models by (mode, policy_hash) to avoid stale validators", "new": "# 按 (mode, policy_hash) 缓存 Pydantic 模型，避免复用过期验证器"},
      {"old": "# Returns: (ImagePrompt, GeneratedImageToolResult, GeneratedImageRecord)", "new": "# 返回：(ImagePrompt, GeneratedImageToolResult, GeneratedImageRecord)"},
      {"old": "\"\"\"Factory: Build Pydantic model from policy dict. Cached by mode and policy_hash.\"\"\"", "new": "\"\"\"工厂函数：根据策略字典构建 Pydantic 模型，并按 mode 与 policy_hash 缓存。\"\"\""},
      {"old": "# Stable schema refs", "new": "# 保持 Schema 引用稳定"},
      {"old": "# Only check for code blocks (triple backticks), not inline formatting", "new": "# 这里只检查代码块（三个反引号），不限制行内格式"},
      {"old": "\"\"\"Tool result contract: validates output from image generation tool.\"\"\"", "new": "\"\"\"工具结果契约：验证图像生成工具的输出。\"\"\""},
      {"old": "\"\"\"Factory: Build DB record model for image storage. Separate from tool result contract.\"\"\"", "new": "\"\"\"工厂函数：构建用于图像存储的数据库记录模型，并与工具结果契约分离。\"\"\""},
      {"old": "\"\"\"DB record contract: validates stored image fields. May drift independently from tool contract.\"\"\"", "new": "\"\"\"数据库记录契约：验证持久化的图像字段，并可独立于工具契约演进。\"\"\""},
      {"old": "\"\"\"Get cached models for a policy. Cache key includes policy_hash to avoid stale validators.", "new": "\"\"\"获取某套策略对应的缓存模型。缓存键包含 policy_hash，以避免复用过期验证器。"},
      {"old": "Returns: (ImagePrompt, GeneratedImageToolResult, GeneratedImageRecord)", "new": "返回：(ImagePrompt, GeneratedImageToolResult, GeneratedImageRecord)"},
      {"old": "# Tool result contract", "new": "# 工具结果契约"},
      {"old": "# DB record contract", "new": "# 数据库记录契约"},
      {"old": "\"\"\"DB record contract with schema versioning and audit trail.", "new": "\"\"\"带 Schema 版本控制与审计轨迹的数据库记录契约。"},
      {"old": "Note: image field accepts any BaseModel instance (GeneratedImageRecord is built dynamically", "new": "注意：image 字段接收任意 BaseModel 实例（GeneratedImageRecord 会动态构建"},
      {"old": "per policy mode, but we enforce it's a validated Pydantic model via type annotation).", "new": "于不同策略模式，但通过类型注解强制要求它必须是经过验证的 Pydantic 模型）。"},
      {"old": "# Typed contract (GeneratedImageRecord instance), validated before construction", "new": "# 类型化契约（GeneratedImageRecord 实例），构造前已经验证"},
      {"old": "# Hash of policy snapshot for audit", "new": "# 策略快照哈希，用于审计"},
      {"old": "# Combined hash of schemas + policy semantics (for migration safety)", "new": "# Schema 与策略语义的组合哈希，用于保障迁移安全"},
      {"old": "# Run identifier tying DB record to LangGraph thread_id and checkpointer state", "new": "# 运行标识，用于把数据库记录关联到 LangGraph thread_id 和 checkpoint 状态"},
      {"old": "\"\"\"Convert to JSON-compatible dict for DB storage using single point of truth.\"\"\"", "new": "\"\"\"通过单一事实来源转换为可写入数据库的 JSON 兼容字典。\"\"\""},
      {"old": "# Defensive check: ensure image is a BaseModel (Pydantic validates, but explicit is clearer)", "new": "# 防御式检查：确保 image 是 BaseModel；虽然 Pydantic 会验证，但显式检查更清晰"},
      {"old": "# Use model_dump for entire record, but explicitly serialize nested image contract", "new": "# 整条记录使用 model_dump，同时显式序列化嵌套的 image 契约"},
      {"old": "# This makes the contract boundary clear: we intentionally serialize the validated image", "new": "# 这样可以明确契约边界：这里只序列化已经验证过的 image"},
      {"old": "# Explicit contract serialization", "new": "# 显式进行契约序列化"},
      {"old": "# Uses additive reducer: every node appends", "new": "# 使用加法 reducer：每个节点都向列表追加内容"},
      {"old": "# Explicit validation status", "new": "# 显式验证状态"},
      {"old": "# Pinned policy for this run", "new": "# 本次运行固定使用的策略"},
      {"old": "# Mode snapshot for this run", "new": "# 本次运行的模式快照"},
      {"old": "# Hash of pinned policy (for cache key and audit)", "new": "# 固定策略的哈希，用作缓存键与审计标识"},
      {"old": "# Pinned max_retries from policy (avoid repeated lookups)", "new": "# 从策略中固定的 max_retries，避免重复查找"}
    ],
    "F91LvgHhgI37": [
      {"old": "\"\"\"Pin policy at START to ensure deterministic validation throughout run.\"\"\"", "new": "\"\"\"在 START 阶段固定策略，确保整次运行中的验证行为具有确定性。\"\"\""},
      {"old": "# Compute policy hash for caching and audit", "new": "# 计算策略哈希，用于缓存与审计"},
      {"old": "# Pre-build and cache models for this policy", "new": "# 预先为该策略构建并缓存模型"},
      {"old": "# Extract policy semantics that affect validation but aren't in JSON Schema", "new": "# 提取会影响验证但不会体现在 JSON Schema 中的策略语义"},
      {"old": "# (e.g., required_keywords, forbid_markdown, require_style_tag)", "new": "# 例如 required_keywords、forbid_markdown、require_style_tag"},
      {"old": "# Normalize aggressively for hash stability", "new": "# 进行充分归一化，以保证哈希稳定"},
      {"old": "# Normalized", "new": "# 已归一化"},
      {"old": "# If style_tags list exists in config, normalize it here too", "new": "# 如果配置中存在 style_tags 列表，也应在这里归一化"},
      {"old": "# Compute combined schema hash: schemas + policy semantics for migration safety", "new": "# 计算组合 Schema 哈希：Schema + 策略语义，用于保障迁移安全"},
      {"old": "# This ensures schema_hash changes when validation behavior changes, even if JSON Schema doesn't", "new": "# 即使 JSON Schema 未变，只要验证行为变化，schema_hash 也会变化"},
      {"old": "# Use run_id from state if present (set from thread_id in config), otherwise generate", "new": "# 若 State 中已有 run_id（由配置里的 thread_id 设置）则直接使用，否则生成一个新的"},
      {"old": "# Store for routing (avoid repeated lookups)", "new": "# 保存到 State 供路由使用，避免重复查找"},
      {"old": "# Run identifier for DB record and checkpoint tracing", "new": "# 数据库记录与 checkpoint 追踪使用的运行标识"},
      {"old": "\"\"\"Agent A: Writes content draft.\"\"\"", "new": "\"\"\"智能体 A：撰写内容草稿。\"\"\""},
      {"old": "\"\"\"Agent B: Proposes image prompt (may be invalid initially).\"\"\"", "new": "\"\"\"智能体 B：提出图像 Prompt；初始结果可能不合法。\"\"\""},
      {"old": "# Will fail validation", "new": "# 这个值会触发验证失败"},
      {"old": "\"\"\"Validation Gate: Enforces Pydantic contract with retry logic.\"\"\"", "new": "\"\"\"验证门：结合重试逻辑执行 Pydantic 契约。\"\"\""},
      {"old": "# Use pinned value from state", "new": "# 使用 State 中固定的值"},
      {"old": "# Explicit success flag", "new": "# 显式成功标志"},
      {"old": "# Increment retry count first, then check (makes log numbering clearer)", "new": "# 先递增重试次数再判断上限，让日志编号更清晰"},
      {"old": "# Update so routing can detect exceeded state", "new": "# 更新计数，使路由能够识别已超过上限的状态"},
      {"old": "\"\"\"Agent D: Refines prompt based on validation error.\"\"\"", "new": "\"\"\"智能体 D：根据验证错误修正 Prompt。\"\"\""},
      {"old": "# Build prompt that satisfies all requirements", "new": "# 构造一个满足全部约束的 Prompt"},
      {"old": "\"\"\"Agent E: Calls image generation tool.\"\"\"", "new": "\"\"\"智能体 E：调用图像生成工具。\"\"\""},
      {"old": "\"\"\"Validation Gate: Validates tool output before DB storage.\"\"\"", "new": "\"\"\"验证门：在写入数据库前验证工具输出。\"\"\""},
      {"old": "\"\"\"Prepares validated record for DB storage with JSON serialization.\"\"\"", "new": "\"\"\"准备经过验证并完成 JSON 序列化的数据库记录。\"\"\""},
      {"old": "# Convert tool result to DB record contract (may have different validation rules)", "new": "# 把工具结果转换为数据库记录契约；两者可以采用不同验证规则"},
      {"old": "# In this example they're identical, but in production they can drift independently", "new": "# 本例中两者相同，但在生产环境中可以独立演进"},
      {"old": "# Typed contract (GeneratedImageRecord BaseModel), not dict", "new": "# 类型化契约（GeneratedImageRecord BaseModel），而不是字典"},
      {"old": "# Use stored hash from init", "new": "# 使用初始化阶段保存的哈希"},
      {"old": "# Run identifier tying DB record to thread_id and checkpointer", "new": "# 用运行标识把数据库记录关联到 thread_id 与 checkpointer"},
      {"old": "\"\"\"Persists validated, JSON-serialized record to DB.\"\"\"", "new": "\"\"\"把经过验证并完成 JSON 序列化的记录持久化到数据库。\"\"\""},
      {"old": "\"\"\"Route based on explicit validation status.\"\"\"", "new": "\"\"\"根据显式验证状态进行路由。\"\"\""},
      {"old": "# Check if we've exceeded max retries (node increments first, then checks)", "new": "# 判断是否已超过最大重试次数；节点会先递增，再检查上限"}
    ],
    "tSZj1kyPJvfi": [
      {"old": "# Note: We print from cfg here for initial display, but actual execution uses pinned values from state", "new": "# 注意：这里为了初始展示从 cfg 读取；真正执行时使用 State 中固定的值"},
      {"old": "# Set run_id from thread_id to tie DB record to LangGraph checkpoint state", "new": "# 从 thread_id 设置 run_id，把数据库记录关联到 LangGraph checkpoint 状态"},
      {"old": "# Tie DB record to LangGraph thread_id", "new": "# 把数据库记录关联到 LangGraph thread_id"},
      {"old": "# Use pinned value from state, not cfg.mode", "new": "# 使用 State 中固定的值，而不是 cfg.mode"},
      {"old": "# Run identifier for tracing", "new": "# 用于追踪的运行标识"},
      {"old": "# Ties DB record to checkpoint", "new": "# 把数据库记录关联到 checkpoint"}
    ],
    "diKdffWL79PZ": [
      {"old": "# Create Instructor client using OpenAI client (compatible with OpenRouter)", "new": "# 创建 Instructor Client；底层使用兼容 OpenRouter 的 OpenAI Client"},
      {"old": "# Instructor works by patching the provider's create() method", "new": "# Instructor 通过 patch 提供商的 create() 方法工作"},
      {"old": "# Architecture: Pydantic Model → Provider Handler → Dispatcher → Provider Client → Retry Layer → Instructor (patched)", "new": "# 架构：Pydantic Model → Provider Handler → Dispatcher → Provider Client → Retry Layer → Instructor（patched）"},
      {"old": "# Create OpenAI client configured for OpenRouter", "new": "# 创建面向 OpenRouter 配置的 OpenAI Client"},
      {"old": "# Instructor automatically retries with validation error feedback", "new": "# Instructor 会携带验证错误反馈自动重试"},
      {"old": "# No need for separate refine nodes or conditional routing", "new": "# 不再需要独立的 refine 节点或条件重试路由"},
      {"old": "# Instructor handles validation internally - no manual retry logic needed", "new": "# Instructor 在内部完成验证，不需要手工编写重试逻辑"},
      {"old": "# The patched client's chat.completions.create() now supports response_model and max_retries", "new": "# patch 后的 Client 支持 response_model 与 max_retries"},
      {"old": "# Flow: create() → retry layer → process_response → validation → reask if needed", "new": "# 流程：create() → retry layer → process_response → validation → 必要时重新提问"},
      {"old": "# When validation fails, Instructor's handle_reask_kwargs() appends error feedback", "new": "# 验证失败时，Instructor 的 handle_reask_kwargs() 会追加错误反馈"},
      {"old": "# was client.chat.completions.create(...)", "new": "# 原调用形式为 client.chat.completions.create(...)"},
      {"old": "# triggers schema/tool wiring + parsing", "new": "# 触发 Schema / Tool 绑定与解析"},
      {"old": "# tenacity-backed validation retries", "new": "# 基于 tenacity 的验证重试"},
      {"old": "# Instructor returns the parsed Pydantic model directly (with _raw_response attached)", "new": "# Instructor 直接返回解析后的 Pydantic 模型，并附带 _raw_response"},
      {"old": "# Access raw provider response if needed: result._raw_response", "new": "# 如有需要，可通过 result._raw_response 访问提供商原始响应"}
    ],
    "oPCW6E4L79PZ": [
      {"old": "# Simplified state - no retry_count, last_error, or prompt_ok needed", "new": "# 简化后的 State：不再需要 retry_count、last_error 或 prompt_ok"},
      {"old": "\"\"\"Generate prompt using Instructor - validation and retry happen here.\"\"\"", "new": "\"\"\"使用 Instructor 生成 Prompt；验证与重试都在这里发生。\"\"\""},
      {"old": "# Instructor handles validation internally, so we get a valid prompt directly", "new": "# Instructor 在内部完成验证，因此这里可以直接拿到合法 Prompt"},
      {"old": "# The patched client automatically retries with validation error feedback", "new": "# patch 后的 Client 会携带验证错误反馈自动重试"},
      {"old": "\"\"\"Generate image - no validation gate needed if Instructor ensures valid input.\"\"\"", "new": "\"\"\"生成图像；如果 Instructor 已保证输入合法，就无需额外输入验证门。\"\"\""},
      {"old": "# Build simplified graph - no refine node, no conditional retry routing", "new": "# 构建简化后的图：没有 refine 节点，也没有条件重试路由"},
      {"old": "# Simple linear flow - no branching for retries", "new": "# 简单线性流程：无需为重试引入分支"}
    ]
  }
}
